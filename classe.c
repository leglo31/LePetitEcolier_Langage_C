#include <stdlib.h>
#include <stdio.h>

#include "eleve.h"
#include "classe.h"

// Retourne nul si pas possible

/*
void afficher(void)
{
    Classe_t *nouvelleClasse(int id, int nbEleves, TypeClasse_t type, char *listeInstit[15]);

    // Libération de mémoire pour Classe_t

    Classe_t *classe = (Classe_t *)malloc(sizeof(Classe_t));

    if (classe != NULL)
    {
        classe->id = id;
        classe->type = type;
    }

    // Répartition des classes

    if (anneeNaissance <= 2015 && anneeNaissance >= 2014)
    {
        return CP;
        printf("%s", listeInstit[0]);
    }
    else if (anneeNaissance <= 2014 && anneeNaissance >= 2013)
    {

        return CE1;
        printf("%s", listeInstit[1]);
    }
    else if (anneeNaissance <= 2013 && anneeNaissance >= 2012)
    {
        return CE2;
        printf("%s", listeInstit[2]);
    }
    else if (anneeNaissance <= 2012 && anneeNaissance >= 2011)
    {
        return CM1;
        printf("%s", listeInstit[3]);
    }
    else if (anneeNaissance <= 2011 && anneeNaissance >= 2010)
    {
        return CM2;
        printf("%s", listeInstit[4]);
    }

    printf("%s", listeInstit[15]);
}

// Afficher une classe

void afficherChasseur(Classe_t *classe);
{
}
*/